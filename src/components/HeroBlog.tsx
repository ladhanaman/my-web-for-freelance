'use client'

export default function HeroBlog() {
  return (
    <section style={{
      width: '100%',
      height: '100vh',
      background: 'transparent',
    }}>
      {/* Content overlaid on terminal background — add hero text/UI here */}
    </section>
  )
}
